ลง Debian 13
> apt update && apt -y upgrade
> หรือตั้งค่า sources.list
> apt -y install nodejs npm docker.io docker-compose openssh-server openssh-client ufw git curl fastfetch micro

# ดึง Script ติดตั้ง Node.js (รุ่น LTS ล่าสุด)
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -

# ติดตั้ง Node.js
sudo apt install -y nodejs

> git clone https://github.com/vactuzx-dot/meox
> config etc/ssh/sshd_config
(ถ้าเพื่อนทำvlanเสร็จไปconfig networkเป็นstatic)
auto ens18
iface ens18 inet static
    address 192.168.10.100/24
    gateway 192.168.10.1

ufw enable
ufw allow OpenSSH
ufw allow 53/tcp
ufw allow 53/udp
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 443/udp
ufw allow 2222/tcp
ufw allow 5000/tcp
ufw allow 5005/tcp
ufw allow 5005/udp
ufw allow 8080/tcp
ufw allow 9443/tcp
ufw status

#ทำทั้ง2VM
micro /etc/docker/daemon.json
{
    "insecure-registries": ["192.168.10.20:5005"]
}

sudo adduser <name>
sudo usermod -aG sudo <name>

whoami

sudo apt update

sudo deluser --remove-home <name>

visudo

<name> ALL=(ALL) NOPASSWD: ALL